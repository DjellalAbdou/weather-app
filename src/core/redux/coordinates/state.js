import { Map } from 'immutable'

export default Map({
  userLocation: Map({}),
  selectedPositionWeather: Map({}),
  currentSelectedDay: Map({}),
  selectedDegreeType: 'C',
  selectedPlace: Map({}),
})